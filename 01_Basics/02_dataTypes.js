"user strict"; // treat all JS code as newer version

// alert(3+3) // we are using NodeJs not browser to run javascript

console.log(3+3); console.log("Pushpak") // code readability is important don't right like this

let name = "Pushpak"
let age = 25
let isLoggedIn = true
let state; // undefined
let nullValue = null; // it means empty

/** Primitive DataTypes */
// number -> 2 to power 53
// bigint
// string -> ""
// boolean -> true or false
// null -> standalone value
// undefined -> when it is not assigned any value
// symbol -> to find uniqueness 

// object
console.log(typeof(undefined)) // undefined
console.log(typeof(null)) // object
